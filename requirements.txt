# ============================================================
# assistant-ia-api — Dépendances Python
# ============================================================

# Framework web
fastapi>=0.111.0
jinja2>=3.1.0

# Serveur ASGI
uvicorn[standard]>=0.29.0

# Templates HTML (interface web servie par l'API)
jinja2>=3.1.0

# Configuration via variables d'environnement
pydantic-settings>=2.2.0
pydantic[email]>=2.0.0
python-dotenv>=1.0.0

# --- Étape 2 : Authentification & Base de données ---
sqlalchemy>=2.0.0
alembic>=1.13.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1           # passlib 1.7.4 incompatible avec bcrypt>=5.0
python-jose[cryptography]>=3.3.0
python-multipart>=0.0.9

# Base de données : SQLite (dev) → PostgreSQL (prod)
psycopg2-binary>=2.9.9

# --- Étape 3 : IA ---
# google-generativeai>=0.7.0
openai>=1.30.0

# --- RAG Vectoriel (Recherche sémantique par Embeddings) ---
# Moteur d'embeddings multilingue (modèle paraphrase-multilingual-MiniLM-L12-v2)
sentence-transformers>=5.0.0
numpy>=1.24.0
# Extraction de texte depuis les fichiers PDF
pymupdf>=1.24.0

# --- Développement & Tests ---
pytest>=8.0.0
httpx>=0.27.0
